---
title: "Non-Linear Models"
chapter: 3
subtitle: "When a straight line isn't enough"
---

# Non-Linear Models

Real-world data rarely follows a straight line.

<Path
  current="Here we keep the same idea from linear regression, but stretch the function so it can curve."
  prev={{
    title: "Linear Regression",
    hint: "We learned how one line can fit simple patterns and how gradient descent moves its parameters.",
  }}
  next={{
    title: "Classification",
    hint: "If the output is not a number but a category, we need a different target.",
  }}
/>

Consider predicting the trajectory of a ball:

$$y = -4.9t^2 + v_0 t + h_0$$

Or diminishing returns on ad spend — doubling your budget doesn't double your sales.

## Why Linear Models Fail

A linear model can only draw straight lines (or flat planes in higher dimensions). But what if the data looks like this?

```
Price
  │        ·  ·
  │      ·
  │    ·
  │  ·
  │ ·
  │·
  │·
  └──────────────── Sqft
```

At a certain point, adding more square footage has diminishing returns on price. A straight line can't capture this curve.

## Try It: Fit a Curve

Try fitting a parabola to the data. First set a=0 to see why a straight line fails, then adjust all three parameters.

<NonlinearDemo />

<NonlinearComparisonDemo />

## Adding Non-Linearity

We can extend our model with polynomial terms:

$$y = w_1 x + w_2 x^2 + b$$

Or with more features:

$$y = w_1 x_1 + w_2 x_2 + w_3 x_1 x_2 + b$$

The training process is identical — gradient descent still works. We just have more parameters to tune.

<Callout type="idea">
The jump from linear to non-linear is crucial: it lets models capture curves, interactions, and complex patterns. But manually choosing the right non-linear terms (is it $x^2$? $\log x$? $\sqrt{x}$?) requires domain expertise.
</Callout>

## The Problem with Manual Feature Engineering

For house prices, maybe $x^2$ works. But what about:
- Recognizing faces in photos (millions of pixels)
- Understanding natural language (infinite combinations of words)
- Predicting protein structures

We can't manually decide which non-linear combinations matter. We need a model that can **learn its own non-linear features**.

> This is exactly what neural networks do — and why they're so powerful.

## From Hand-Crafted to Learned Features

| Approach | Non-Linearity | Effort |
|----------|--------------|--------|
| Linear regression | None | Zero |
| Polynomial regression | $x^2, x^3, \ldots$ | Manual choice |
| Feature engineering | $\log x$, interactions | Domain expertise |
| **Neural networks** | **Learned automatically** | **Just provide data** |

<Callout type="analogy">
Hand-crafting features is like writing specific rules: "if the email contains 'Nigerian prince', it's spam." Neural networks instead learn: "here are patterns I've found across millions of emails that correlate with spam." Much more general.
</Callout>

We'll get to neural networks in Chapter 5. But first — what if the output isn't a number?

<Exercise title="Tiny Challenge">
Explain why the same dataset can look fine to a quadratic model but awkward to a line.
</Exercise>

<Callout type="idea" title="Takeaway">
Non-linearity gives a model enough shape to follow real-world curves and interactions.
</Callout>
