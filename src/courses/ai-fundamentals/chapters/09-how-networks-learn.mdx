---
title: "How Networks Learn"
chapter: 9
subtitle: "Forward pass, backward pass, validation, and the training loop"
---

# How Networks Learn

<ChapterBridge
  why="This chapter shows that neural-network training is not a new kind of magic. It is the same loop as before, just with many more parameters and a smarter credit-assignment step."
  buildsOn="The training loop for simple models and the idea of hidden representations."
  unlocks="Modern deep-learning training, overfitting awareness, and later LLM training concepts."
  map={["Forward pass", "Loss", "Backward pass", "Validation"]}
  trackFocus={{
    conceptual: "Backpropagation is best learned as credit assignment before it is learned as calculus.",
    builder: "This is the chapter where optimization and generalization finally meet."
  }}
/>

The training loop for neural networks is the same loop we already saw with a line:

1. make predictions
2. measure error
3. adjust parameters
4. repeat

The new challenge is scale. There are many more parameters, across many more layers.

## Forward Pass and Backward Pass

- **forward pass**: compute the prediction
- **loss**: measure how wrong it was
- **backward pass**: figure out how each parameter contributed to that error
- **update**: nudge the parameters

The formal name for that backward credit assignment is **backpropagation**.

<Callout type="idea">
Backpropagation is best understood as “how blame and credit move backward through the network,” not as a wall of calculus symbols.
</Callout>

## Guided Lab

The notebook below trains a tiny neural network directly in the browser.

<NeuralNetworkNotebook />

## Training vs Validation

A model can look good on training examples and still fail on new ones.

That is why we separate:

- **training data**: used to update the model
- **validation data**: used to check whether the model generalizes

<TrainingCurveDemo />

<CaseCards
  title="Why generalization matters"
  cards={[
    {
      title: "Customer-support routing model",
      product: "Incoming ticket text becomes a routing decision for the right team.",
      modelIdea: "The model needs to perform well on new tickets, not just the ones it already saw during training.",
      failureMode: "A model can memorize wording patterns from historical tickets and then collapse when new product names or issue types arrive.",
      trackPrompt: {
        builder: "Validation is the reality check that stops you from trusting training loss alone."
      }
    },
    {
      title: "Student-success predictor",
      product: "Behavioral and course signals become a dropout-risk score.",
      modelIdea: "The model's value depends on whether it generalizes to future cohorts, not whether it memorizes last semester.",
      failureMode: "An overfit model can look brilliant on old data and useless on the next term's students.",
      trackPrompt: {
        conceptual: "Notice the trap: good-looking training performance can still hide a weak model."
      }
    }
  ]}
/>

<Exercise title="Tiny Challenge" hint="The overfit setting looks best on training data (lowest training loss), but the balanced setting gives the best validation story because it generalizes without memorizing noise.">
Switch between underfit, balanced, and overfit. Which setting looks best on training data? Which one gives the best validation story?
</Exercise>

<Checkpoint
  id="ch9-validation"
  chapter={9}
  prompt="Why do we keep validation data separate from training data?"
  concepts={["generalization", "validation", "overfitting"]}
  options={[
    {
      label: "Because we need a check on whether the model works on unseen examples instead of just the data it practiced on.",
      correct: true,
      explanation: "Exactly. Validation is how we test whether the learned pattern transfers beyond the training examples."
    },
    {
      label: "Because training data is only for measuring loss and should never update parameters.",
      explanation: "That is backward. Training data is precisely what updates the parameters."
    },
    {
      label: "Because validation data makes backpropagation mathematically simpler.",
      explanation: "Validation is about trustworthy evaluation, not simplifying the gradient calculation."
    }
  ]}
/>

<Checkpoint
  id="ch9-backprop"
  chapter={9}
  prompt="What does backpropagation do?"
  concepts={["backpropagation", "gradients", "training"]}
  options={[
    {
      label: "It sends the error signal backward through the network so every layer knows how to adjust its weights.",
      correct: true,
      explanation: "Right. Backpropagation distributes the blame for errors across all layers efficiently."
    },
    {
      label: "It sends data backward through the network to generate new training examples.",
      explanation: "Backpropagation moves error signals, not data. It computes gradients for parameter updates."
    },
    {
      label: "It removes layers that are not contributing to the correct answer.",
      explanation: "Backpropagation adjusts weights, not architecture. All layers remain and get updated."
    }
  ]}
/>

<Checkpoint
  id="ch9-overfit-signs"
  chapter={9}
  prompt="What is the clearest sign of overfitting?"
  concepts={["overfitting", "generalization", "training curve"]}
  options={[
    {
      label: "Training loss keeps dropping but validation loss starts rising.",
      correct: true,
      explanation: "Yes. The growing gap between training and validation performance is the classic overfitting signature."
    },
    {
      label: "Both training and validation loss are high and not improving.",
      explanation: "That is underfitting — the model lacks capacity or is not learning at all."
    },
    {
      label: "The model trains very slowly regardless of learning rate.",
      explanation: "Slow training can have many causes. Overfitting is specifically about the train/validation gap."
    }
  ]}
/>

<Callout type="idea" title="Takeaway">
Training a neural network is still the same prediction-measure-update loop. The difference is that we are now adjusting many layers of learned detectors.
</Callout>
