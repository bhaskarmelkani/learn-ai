---
title: "Predicting Numbers with a Line"
chapter: 3
subtitle: "The simplest useful model for number prediction"
---

# Predicting Numbers with a Line

<ChapterBridge
  why="A line is the cleanest first trainable model because you can see exactly what each learned number does."
  buildsOn="The training loop and the idea that a model is just a function with parameters."
  unlocks="Loss, gradient descent, and the later jump to more flexible model shapes."
  map={["Input x", "Weight w", "Bias b", "Predicted y"]}
  trackFocus={{
    conceptual: "This chapter is about physical intuition. Tilt and lift matter more than memorizing notation.",
    builder: "Notice how little machinery is needed before a model already feels useful."
  }}
/>

Return to the house-price question.

If we know a few past sales and want to guess the next one, the simplest instinct is to draw a line through the pattern.

That idea has a name: **linear regression**.

## The Line Story

The line has a very small job description:

- one number controls the **tilt**
- one number controls the **baseline**

Later we call them **weight** and **bias**, but first just feel what they do.

<LinearRegressionDemo />

## Naming the Parts

Once the line feels natural, we can finally name it:

$$y = wx + b$$

- $x$ = input
- $y$ = predicted output
- $w$ = weight
- $b$ = bias

So linear regression is just a model that uses a straight line to predict a number.

## Why Start Here?

Because this is the clearest first example of a trainable model:

- it makes a prediction
- it has parameters
- we can see the effect of changing them
- we can later learn how those parameters get adjusted automatically

<Callout type="idea">
The important intuition is not "memorize the line equation." It is "a model can be a very simple shape with a few learned numbers."
</Callout>

<Checkpoint
  id="ch3-weight-tilt"
  chapter={3}
  prompt="What does the weight do in the line?"
  concepts={["linear regression", "weight", "bias"]}
  options={[
    {
      label: "It changes the tilt, so predictions rise or fall faster as the input changes.",
      correct: true,
      explanation: "Right. Weight controls the slope of the line: how strongly the output changes as the input changes."
    },
    {
      label: "It lifts the whole line upward without changing the slope.",
      explanation: "That is the job of the bias. Bias shifts the baseline; weight changes the angle."
    },
    {
      label: "It changes which data points are used for training.",
      explanation: "Parameters change the model's behavior, not the membership of the dataset."
    }
  ]}
/>

<Checkpoint
  id="ch3-bias-role"
  chapter={3}
  prompt="What does the bias do in the line equation y = wx + b?"
  concepts={["linear regression", "bias", "parameters"]}
  options={[
    {
      label: "It shifts the whole line up or down without changing the slope.",
      correct: true,
      explanation: "Right. Bias sets the baseline — where the line crosses zero input."
    },
    {
      label: "It controls how steeply the line rises or falls.",
      explanation: "That is the weight's job. Bias only moves the line vertically."
    },
    {
      label: "It determines how many data points are used for training.",
      explanation: "Bias is a model parameter, not a data selection mechanism."
    }
  ]}
/>

<Checkpoint
  id="ch3-why-line-first"
  chapter={3}
  prompt="Why is a straight line a good first model to study?"
  concepts={["linear regression", "model complexity", "learning"]}
  options={[
    {
      label: "Because it has very few parameters, so you can see exactly what each one does.",
      correct: true,
      explanation: "Yes. With just a weight and a bias, the effect of changing each parameter is immediately visible."
    },
    {
      label: "Because straight lines can fit any real-world dataset perfectly.",
      explanation: "Lines cannot fit curved patterns. That is why later chapters introduce non-linear models."
    },
    {
      label: "Because it is the only model that can be trained with gradient descent.",
      explanation: "Gradient descent works on many model shapes, not just lines."
    }
  ]}
/>

<Callout type="idea" title="Takeaway">
Linear regression is the first concrete example of a model learning to predict a number from data.
</Callout>
